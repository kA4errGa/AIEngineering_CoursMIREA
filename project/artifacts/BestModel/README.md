# Выбранная модель

- Сохранить саму модель в репозиторий не удалось из-за занимаемого ей объёма (131 мб). Поэтому обучение и сохранение модели `best_model.joblib` происходит на этапе сборки проекта с помощью Docker.
- В этой дириктории сохранены метрики модели `TestEval.csv`, а также в  `configs\best_model_config.json` сохранены настройки получаемой модели.