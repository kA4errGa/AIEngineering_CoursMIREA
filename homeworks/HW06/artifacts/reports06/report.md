# EDA-отчёт

Исходный файл: `S06-hw-dataset-03.csv`

Строк: **15000**, столбцов: **30**

## Качество данных (эвристики)

- Оценка качества: **1.00**
- Макс. доля пропусков по колонке: **0.00%**
- Слишком мало строк: **False**
- Слишком много колонок: **False**
- Слишком много пропусков: **False**
- Колонки с одинаковыми значениями: **False**
- Категориальные колонки с большим числом значений (0.85 или более уникальных записей от общего числа): **False**

## Колонки

См. файл `summary.csv`.

## Пропуски

См. файлы `missing.csv` и `missing_matrix.png`.

## Корреляция числовых признаков

См. `correlation.csv` и `correlation_heatmap.png`.

## Категориальные признаки

Категориальные/строковые признаки не найдены.

## Гистограммы числовых колонок

См. файлы `hist_*.png`.
